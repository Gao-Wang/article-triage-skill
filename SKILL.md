---
name: article-triage
description: 文章/项目打分收藏。用户发来链接（微信公众号文章、GitHub 开源项目、AI 玩法等）时：抓取内容 → 生成摘要 → 按用户兴趣画像打分(0-10) → 写入 Readeck 打多维标签 → 回复一屏总结。也处理"校准"命令（基于 Readeck 强信号提出画像修改建议）。
metadata:
  hermes:
    tags: [收藏, 打分, Readeck, 摘要, 兴趣画像]
---

# 文章打分收藏（Article Triage）

## 触发条件

用户发来一个链接（mp.weixin.qq.com / github.com / 其他文章或项目 URL），
意图是"帮我看看这个值不值得 / 收藏一下"。

## 环境

- Readeck API：`$READECK_URL`（必填，如 `http://<readeck-host>:8080`），认证头 `Authorization: Bearer $READECK_TOKEN`
- 辅助脚本：`scripts/readeck.mjs`（封装去重/创建/备注/删除/按标签查询）
- 兴趣画像：`profile.md`（本目录）

## 工作流

1. **提取 URL**（可能有微信跳转参数，保留完整 URL）。
2. **去重**：`node scripts/readeck.mjs find "<url>"`。
   - 已存在 → 不重复写入，告知"这篇之前存过，当时评分 X、标签 XXX"，并问是否重新评估；用户同意才走 7。
3. **抓取内容**：
   - 公众号文章（mp.weixin.qq.com）：用网页抓取工具获取正文。
   - GitHub 项目：抓 README 要点 + star 数 + 技术栈 + license + 最近更新时间。
   - 其他：抓正文。
   - **失败** → 跳过写入，告知用户原因，流程结束。
4. **摘要**：200–300 字，覆盖"是什么 / 解决什么问题 / 技术栈 / 亮点"。
5. **打分**：对照 `profile.md` 的评分标准打 **0–10 整数分**，附一句话理由。
6. **标签**：按下面的标签体系确定（内容域 + 优先级 + 硬件匹配）。
7. **写入 Readeck**：
   - 一步完成：`node scripts/readeck.mjs add "<url>" --title "标题" --labels "标签1,标签2,..." --note "摘要 + 评分 + 理由"`
   - 若返回"note write timed out"：稍后补 `node scripts/readeck.mjs note <id> "..."`（先 find 拿 id）
8. **回复一屏总结**（中文，≤10 行）：标题 / 类型 / 评分 / 标签 / 一句话理由 / 是否已保存。
   **回复中严禁展示**任何命令、代码块、脚本路径（如 `node scripts/...`）、API 细节或技术过程——所有操作都在后台静默完成，用户看到的只有简洁的结论。

## 评分标准

- 基础分由与画像兴趣域的匹配度决定（见 `profile.md`）。
- **加分**：手头机器能跑（见 `profile.md` 硬件环境）、上手成本低、与用户当前工作相关、GitHub star 高且维护活跃。
- **减分**：与兴趣域无关、依赖太重、需专用硬件、内容过时。
- **档位**：≥8 → `#高优先`；4–7 → `#稍后读`；<4 → `#归档`（**低分也照常保存**）。
- 输出：整数分 + 一句话理由。

## 标签体系

- **内容域**：`#AI` `#编程` `#工具` `#Agent技能` `#硬件/部署` `#其他`（可按用户领域自定义）
- **优先级**：`#高优先` `#稍后读` `#归档`
- **硬件匹配**：`#可跑Mac` `#可跑Ubuntu`
- 可补充更贴切的具体标签（如 `#docker`、`#自托管`）。

## 校准（用户说"校准"或定期提醒时）

1. 用 `scripts/readeck.mjs list --since 7d` 拉取近期书签，识别**强信号**：
   - 正面：`is_marked`（打星）、写了 note、手动改过标签
   - 负面：`is_deleted`、`is_archived`
2. 汇总成极简清单（高分未读 / 低分已读 / 用户标记过的）。
3. 提出**画像修改建议**（≤3 条），**等用户确认后**才更新 `profile.md`；
   更新前备份为 `profile.md.bak-<日期>`，支持回滚。

## 注意事项

- 凭据一律走环境变量（`READECK_URL` / `READECK_TOKEN`），不写死在技能里。
- 尊重去重；抓取失败跳过并告知。
- 回复保持简洁中文，不堆砌；绝不出现命令、代码、脚本路径或实现细节，只输出用户能直接看懂的结果。
